void main() {
  int x = 15;
  double y = 5.5;
  print(x);
  print(y);
  //*int resalt =x+y; A value of type 'double' can't be assigned to a variable of type 'int'.
  double z = x + y;
  print(z);
  num x1 = 15;
  num y1 = 5.5;
  print(x1 + y1);
  //!int c=x1+y1;A value of type 'num' can't be assigned to a variable of type 'int'.
//*double e=x1+y1;A value of type 'num' can't be assigned to a variable of type 'double'.
}
